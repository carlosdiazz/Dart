//library areas;

double triangulo({required double base, required double altura}) {
  return (base * altura) / 2;
}

double cuadrado({required double lado}) => lado * lado;

double circulo({required double radio}) => 3.141681 * radio;
